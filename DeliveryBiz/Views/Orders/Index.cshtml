@using DeliveryBiz.Models;

<h1>Orders to Fufill</h1>
@if (Model.Count == 0)
{
  <p>There are no Orders to fufill! Get out there and get their business.</p>
}
<ul>
  @foreach (Order order in Model)
  {
    <li><a href="/orders/@order.Id">@order.Description</a></li>
  }
</ul>

<a href="/orders/new">Add a new Order.</a>

<form action="/orders/delete" method="post">
  <button type="submit" name="button">Clear All Orders</button>
</form>
